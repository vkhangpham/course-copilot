# Audit notes (bd-ccopilot-yuy)

## Pass 2 (ongoing)
- Portal: verified new `_safe_resolve` calls for teacher trace + course plan. Tests green.
- Next focus: run frontend `RunDetailSection`/`RunHistory` outputs to ensure new backend responses render without null issues.
