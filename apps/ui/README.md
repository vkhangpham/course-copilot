# UI placeholder

This directory will eventually host the thin operator UI or glue code for the
Open Notebook front-end. For now, see `vendor/open-notebook` for the canonical
Next.js + FastAPI implementation and keep any new UX-specific files isolated in
this subtree.
