# Learner-specific constraint snapshots

Drop scenario-specific YAML files here if you need to run the pipeline with
alternate learner personas. The default configuration lives under
`config/constraints.yaml`.
